# Eval report (v20260920) — protocol frozen, fine-tune not trained

**SHIP: NO**

- Protocol: `docs/llm-eval-protocol.md` (frozen 2026-09-20)
- Candidate INT4 fine-tune: **not produced** (SageMaker / g5.2xlarge job has not run)
- Baseline comparison on GPU: **not produced**
- On-device gate 5: **not measured** (prior on-device baseline fields are null)

This date locks the protocol, dataset generator, and harness. It is **not** a product ship.

## What was verified on 2026-09-20

| Check | Result |
| --- | --- |
| Dataset mix | train 12000 (7200 SMS / 3000 chat / 1800 general); val 1726; test 1531 (1351 SMS) |
| Split unit | `(bank, template_id)` with seed 42 |
| Metric unit tests | 6/6 passed (`python -m eval.test_metrics`) |
| Dummy harness | gold copy vs empty JSON; see `v20260920-harness.md` (not a model score) |
| Chat eval set | 50 frozen items in `ml/eval/fixtures/chat_eval.jsonl` |
| Dataset hashes | `ml/data/manifest/SHA256SUMS` |

Dummy `SHIP: true` in the harness file only means “gold labels beat a `{}` baseline.” Ignore it for Android `MODEL_URL`.

## Gates (product)

1. R-EM +3 pp vs Qwen3-base — **not run**
2. Amount EM non-inferiority — **not run**
3. Chat groundedness non-inferiority — **not run**
4. JSON validity ≥ 95% on INT4 — **not run**
5. On-device ≤ 1.3× prior baseline — **not run**

## App runtime (interim)

`:ondeviceai` now loads LiteRT-LM `.litertlm`. Until this report is replaced by a GPU run with **SHIP: YES**, `Constants.ModelDownload.MODEL_URL` points at the public **Qwen3-1.7B Instruct INT4** LiteRT Community file (not the FinnDot LoRA). CloudFront `OnDeviceModelUrl` is reserved for the fine-tune.

## Next command

See `docs/aws-ondevice-slm.md`: quota → CDK → `sft_qlora.py` → `export_litertlm.py` → `eval.run_eval --backend hf`.
